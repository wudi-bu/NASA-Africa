###### Build Properties ######
# top-level folder where temporary build files should go
build.base.dir=$build

# low-level folder where temporary build files should go
build.dir=${build.base.dir}/bin

# folder where temporary dist files should go
dist.dir=$dist

# the name of the folder that owns the WEB-INF, et al, folders
web.dir.name=WebContent

# directory in the project where all the external JARs are 
lib.dir=../${web.dir.name}/WEB-INF/lib
jdk.lib.dir=/Library/Java/JavaVirtualMachines/jdk1.8.0_111.jdk/Contents/Home/jre/lib
tomcat.lib.dir=/Users/anindya/Documents/apache-tomcat-8.5.6/lib

# directory where the source code is
src.dir=../src

# name of the WAR to be created
war.name=AgericulturalMap.war